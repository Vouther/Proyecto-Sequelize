<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= movie.title %> </title>
    <link rel='stylesheet' href='/css/style.css' />

</head>
<body>
    <figure>
        <img src="/img/logo-DH.png" alt="Logo Digital House">
    </figure>
    <h1><%= movie.title %> </h1>    
    <br>
    <p>RATING: <%= movie.rating %> </p>
    <p>AWARDS: <%= movie.awards %> </p>
    <p>LENGTH: <%= movie.length + ' min'%> </p>
    <p>RELEASE DATE: <%= movie.release_date %> </p>
    <p>GENRE: <%= movie.genero.name %> </p>
    <p> ACTORS:
        <ul>
            <% movie.actores.forEach(actor => { %>
                <li>
                    <%= actor.first_name %>
                </li>
            <% }) %>
        </ul>
    </p>
    
    <br> 
    <section>
        <a href="/movies/edit/<%= movie.id %>">
            <button class="botonAgregar" type="button">Editar</button>
        </a>
        <a href="/movies/delete/<%= movie.id %>">
            <button class="botonAgregar" type="button">Eliminar</button>
        </a>
    </section>

    <br>
    <a  class="botonVolver" href="/movies">Listado de Películas</a></p>
</body>
</html>